---
label: Work Page
hide_body: true
fields:
- name: featured_work_count
  type: number
  default: 2
  required: true
  config:
    min: 
    max: 
    step: 
  label: Featured Work Count
  description: Choose how many pieces of work can be featured
- name: initial_other_work_count
  type: number
  default: 3
  required: true
  config:
    min: 
    max: 
    step: 
  label: Initial Other Work Count
  description: Choose how many other pieces of work are initially shown
- name: work_order
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: work-07e433be-de0b-488c-8170-e5bf2129eef8
  label: Work Order
  description: The order that work should be displayed in.
pages:
- content/pages/work.md
